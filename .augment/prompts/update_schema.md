Make sure to update the `.augment/schema.mysql.sql` with the recent updates and for it to hold to current, working database and tables configuration.

Update the swagger UI and JSON on 

Update github repo and merge to main
